# the layout file to load with this yaml
layout: index.hbs

# By default, the outputfile route will be
# the relative position of the yaml inside the content folder
# But you can customize the route like this:
# route: customFolder/customPage.html

title: bro-start
description: your starterkit is ready!
message: |
  bro-start is working!
  have fun.